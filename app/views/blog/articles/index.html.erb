<% if admin? %>
  <div>
    <%= link_to 'New article', new_blog_article_path %>
  </div>
<% end %>

<div id="articles" class="transitions-enabled infinite-scroll clearfix">
  <% @articles.each do |article| %>
    <div class="box">
      <div class="panel panel-warning">
        <%= link_to image_tag(article.img_url), blog_article_path(article) %><br/>

        <div class="panel-body" >
          <%= article.title %><br/>
        </div>

        <% if admin? %>
          <div class="panel-footer" >
            <%= link_to 'Edit', edit_blog_article_path(article) %><br/>
            <%= link_to 'Destroy', [:blog, article], method: :delete,
              data: { confirm: 'Are you sure?' } %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
